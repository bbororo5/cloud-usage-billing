# Architecture Test Plan

아키텍처 드라이버를 구현 전 테스트 계획과 연결하고, 구현 후 자동으로 검증한다.

## 테스트

| 대상 | 검증 | 통과 기준 | 실행 시점 |
|---|---|---|---|
| DR-01 | 중복·지연·순서 변경 처리와 배치 비교 | 중복 0건, 금액 차이 0원 | 매 커밋 |
| DR-02 | 초당 330건 지속 입력과 1,000건 최대 입력 | 유실 0건, 99%가 10초 이내 반영 | 주요 변경 후 |
| DR-03 | 1억 건의 월간 배치 중 대시보드 400명과 비용 탐색 100명 조회 | 대시보드 p95 1초, 탐색 p95 2초, 데이터 지연 5분 이하, 진행 중 결과 노출 0건 | 성능 검증 시 |
| DR-04 | 스트리밍·배치 강제 중단 후 재실행 | 중복 0건, 정상 실행과 차이 0원 | 매 커밋·주요 변경 후 |
| DR-05 | 다른 회사의 원본·집계·정산·캐시 접근, 범위 없는 조회, ClickHouse Row Policy 교차 조회 차단, BFF의 원시 원장 접근 거부 | 노출 0건, 비인가 접근 거부 100% | 매 커밋 |
| DR-06 | 역할별 API 호출과 Admin 권한 제거 후 재호출 | 무권한 처리 0건, 이력 누락 0건 | 매 커밋 |
| 점유 귀속 | 회사–VM 점유 구간 `[validFrom, validTo)` 기반 시간 귀속 정확성 | 오귀속 0건 | 매 커밋 |
| 귀속 격리 | 미귀속·중복 점유 이벤트 격리 및 월간 확정 차단 | 미귀속 상태 확정 0건, 오류 격리 100% | 매 커밋 |
| 점유 캐시 | 변경 전 읽기의 늦은 캐시 쓰기, DB 반영 후 캐시 갱신 실패·프로세스 재시작, 진행 중 귀속의 공개 경합 | 버전 역행 0건, 변경 구간 오귀속·미검증 공개 0건, 복구 후 재처리 가능 | 구현 후 매 커밋·통합 검증 |
| 과거 귀속 | 점유 변경 후 이전 회사 사용량 지연 도착 및 과거 이력 캐시 누락 | 원장 조회로 이전 회사에 정확히 귀속, 이벤트 폐기 0건 | 구현 후 매 커밋 |
| 점유 생명주기 | 예약 확보·종료·유휴·재점유, 종료 후 캐시 반영 실패, 유휴 구간 이벤트 | 동시 점유·실패에 따른 과금 연장 0건, 유휴 이벤트 보존·격리, 반영 불완전 중 재할당 차단 | 구현 후 매 커밋·통합 검증 |
| 점유 요청 만료 | 60초 경계의 활성화·만료 경합, 기한 경과 후 재전달, 완료 응답 유실 | 만료 후 신규 활성화·중복 점유 0건, 같은 요청 결과 유지 | 구현 후 매 커밋·통합 검증 |
| 종료 복구 | 해제 후 Kafka ACK 전 중단, 원장 반영 후 캐시 실패, 이전 점유 종료의 지연 도착 | 종료 사실 복구, 실제 해제 시각 유지, 새 점유 종료 0건, 반영 완료 전 offset 진행 0건 | 구현 후 통합 검증 |
| 구조 | 모듈 책임과 의존 관계 검사 | 금지된 의존 0건 | 매 커밋 |

## 운영

| 구분 | 범위 |
|---|---|
| 빠른 테스트 | 작은 데이터로 정확성·재처리·회사 분리·점유 귀속·역할 권한을 매 커밋 검증 |
| 통합 테스트 | 실제 데이터 흐름의 실패·복구·미귀속 차단을 주요 변경 후 검증 |
| 성능 테스트 | 처리량·동시 조회·대용량 성능을 별도로 실행하고 기록 |

## 준비 순서

1. 이벤트 형식 확정 후 테스트 이벤트와 발생기를 만든다.
2. 모듈 책임 확정 후 구조 테스트를 만든다.
3. [API 계약과 권한표](api-contract.md)를 기준으로 권한·부하 테스트를 만든다.
4. 점유 이력 매핑, 미귀속 차단, 테넌트 격리(Row Policy/RLS), 스트리밍·배치 재실행 테스트를 만든다.

모든 ADR에는 관련 테스트와 통과 기준을 함께 기록한다.

## 현재 자동화 기반

| 대상 | 파일 | 현재 검증 |
|---|---|---|
| PostgreSQL | `database/postgresql/schema_test.sql` | RLS, 마지막 Admin, 가격 구간, 실행 재시도, 단일 확정 |
| ClickHouse | `database/clickhouse/schema_test.sql` | 전달 중복 제거, VM 출처 격리, 가격 사본 버전 |
| Kafka | `scripts/verify-kafka-durability.sh` | 복제 계수 3, 최소 ISR 2, 복제본 장애 중 ACK와 ISR 미달 쓰기 거부 |
| 조회·배치 | `database/clickhouse/queries` | 귀속 조회 모델 확정 후 비용·월간 총액·안정 커서 쿼리 추가 |
| 이벤트 계약 | `contracts/v1` | JSON Schema와 예제 일치 |
| API 계약 | `contracts/openapi.yaml` | OpenAPI lint |

수집 파이프라인 검증을 안정화한 뒤 Testcontainers 기반 통합 테스트와 CI 명령으로 묶는다.
